export interface ProductModel {
  id: string;
  name: string;
  price: number;
  description: string;
  imageUrl: string;
  inStock: number;
}

// dùng cho việc đinh nghĩa các kiểu dữ liệu phải có trong san phẩm, ko được thừa hay thiếu
